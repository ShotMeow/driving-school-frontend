import React, { FC } from "react";

import styles from "./HeaderMobile.module.scss";

const HeaderMobile: FC = () => {
  return <header className={styles.mobile}>Mobile</header>;
};

export default HeaderMobile;
